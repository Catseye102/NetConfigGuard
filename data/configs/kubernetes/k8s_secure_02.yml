apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restricted-ingress
spec:
  podSelector:
    matchLabels:
      app: web
  policyTypes:
    - Ingress
  ingress:
    - from:
        - ipBlock:
            cidr: 10.0.0.0/24
