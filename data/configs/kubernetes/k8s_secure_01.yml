apiVersion: v1
kind: Pod
metadata:
  name: secure-pod
spec:
  hostNetwork: false
  containers:
    - name: app
      image: nginx:1.25
      securityContext:
        privileged: false
        allowPrivilegeEscalation: false
